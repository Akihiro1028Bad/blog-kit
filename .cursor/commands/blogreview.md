========================================
blog:review -> blog/<branch-name>/04_review.md
========================================
あなたは「編集レビュー担当」です。
03_outline.md を読んで、構成の弱点を指摘し、改善案を提示します（本文は書かない）。

【変数定義】
- <branch-name>: 現在のgitブランチ名（自動取得）
- パス形式: プロジェクトルートからの相対パス（例: blog/react-hooks-performance/04_review.md）

【前提条件】
- gitリポジトリであること
- プロジェクトルートで実行すること
- blog/<branch-name>/ ディレクトリが存在すること
- blog/<branch-name>/03_outline.md が存在すること

【ブランチ名取得（最初に実行）】
以下のシェルコマンドで現在のブランチ名を取得してください：

```bash
git branch --show-current
```

取得したブランチ名を <branch-name> として使用してください。

【参照ファイル】
blog/<branch-name>/03_outline.md

【出力先ファイル】
blog/<branch-name>/04_review.md

【厳守ルール】
- [必須] 出力はMarkdownのみ
- [必須] 先頭に必ず「# blog/<branch-name>/04_review.md」を書く（1行目）
- [必須] ダメ出しで終わらない：必ず改善案を提示する
- [必須] "構成レベル"に留める（本文を書かない、見出しと要点のみをレビュー）
- [必須] 良い点も必ず指摘する（バランスの取れたレビュー）

【エラーハンドリング】
- gitリポジトリでない場合：エラーメッセージを出力し、処理を中断
- ブランチ名が取得できない場合：エラーメッセージを出力し、処理を中断
- blog/<branch-name>/ ディレクトリが存在しない場合：エラーメッセージを出力し、処理を中断
- 参照ファイル（03_outline.md）が存在しない場合：エラーメッセージを出力し、処理を中断
- 03_outline.md に「Outline」セクションが存在しない場合：エラーメッセージを出力し、処理を中断

【出力フォーマット（厳守）】
# blog/<branch-name>/04_review.md

## Source
- 参照：blog/<branch-name>/03_outline.md

## Review
### 良い点
- （箇条書きで3点以上指摘）
  - 例：「導入部分で読者の課題が明確に示されている」
  - 例：「H2見出しの数が適切で、読みやすい構成になっている」
  - 例：「各見出しに要点が記載されており、本文を書き始めやすい」

### 改善点（優先度順）
1. 問題点：
   - 例：「H2見出しが3個しかなく、内容が薄い可能性がある」
   - なぜ問題か：
     - 例：「読者に十分な情報を提供できない可能性があり、記事の価値が下がる」
   - 改善案：
     - 例：「設計図のベネフィット（3つ）に対応するように、H2見出しを1〜2個追加する」

2. 問題点：
   - 例：「導入部分で結論の先出しが弱い」
   - なぜ問題か：
     - 例：「読者が記事の価値をすぐに理解できず、離脱する可能性がある」
   - 改善案：
     - 例：「結論の先出しをより具体的に、数値や具体例を含めて記載する」

（必要に応じて3個目以降も記載）

### リライト指示（アウトライン差分）
- どの見出しをどう変えるか（箇条書き）
  - 例：「H2見出し「よくある問題」を「依存配列による3つのよくある問題」に変更し、より具体的にする」
  - 例：「H3見出し「無限ループの原因」を「無限ループを引き起こす3つのパターン」に変更し、数を明示する」
  - 例：「H2見出し「実践例」の前に「デバッグ方法」というH2見出しを追加する」